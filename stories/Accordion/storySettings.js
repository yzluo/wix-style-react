import { Category } from '../storiesHierarchy';

export const storySettings = {
  kind: Category.COMPONENTS,
  category: Category.COMPONENTS,

  storyName: 'Accordion',
  dataHook: 'story-accordion',
};
